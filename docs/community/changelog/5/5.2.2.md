---
title: "Changelog 5.2.2"
description: "Release notes for ArchiPy 5.2.2"
---

# 5.2.2 — 2026-09-07

[← 5.2.1](5.2.1.md) | [↑ 5.x series](index.md)


## Added

### Helpers - OpenTelemetry

- **Explicit W3C textmap propagator** - `OtelUtils` installs a TraceContext +
  Baggage composite via `set_global_textmap` on init and test configure
  (idempotent), locking `traceparent` / `tracestate` / `baggage` carriers.

## Tests

### Tests - OpenTelemetry

- **Async gRPC metrics BDD** - `@async` coverage for `create_async_grpc_app`
  `rpc.server.duration`, prepend order, and metrics-only mode
- **Parent-chain BDD** - FastAPI → gRPC hop asserts RPC span descends from
  `GET /call-grpc`
- **Log ↔ trace BDD** - Exported log records carry ambient `trace_id` / `span_id`

## Docs

### Docs - Observability

- **Propagator note** - Document W3C TraceContext + Baggage install on
  `init_otel_if_needed`
